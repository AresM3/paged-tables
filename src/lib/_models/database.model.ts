/**
 * @interface DatabaseModel
 * @description Un modello con le proprietà base presenti in un database
 */
export interface DatabaseModel {
  id?: number;
  created_at?: string;
  updated_at?: string;
}
